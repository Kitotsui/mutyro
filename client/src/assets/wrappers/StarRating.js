import styled from "styled-components";

const Wrapper = styled.div`
  .star-rating {
    color: #ffc107;
    display: inline-flex;
    gap: 1px;
  }
  .star-rating i {
    font-size: 0.75rem;
    line-height: 1;
  }
`;
export default Wrapper;
